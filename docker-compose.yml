version: '3'
services:
  web:
    build:
      context: ./
    volumes:
      - .:/app
      - ./node_modules:/app/node_modules/
    ports:
      - "3000:3000"
    command: [sh, -c, "npm install && npm run startdev"]